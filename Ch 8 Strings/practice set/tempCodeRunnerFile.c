   int cmp = strcmp(arr, arr2);

    if (cmp == 0) {
        printf("\nBoth the entered strings are equal.\n");
    } else {
        printf("\nBoth the strings entered are not equal.\n");
    }
    return 0;
